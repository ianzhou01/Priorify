// Priorify configuration

const TIMER_DURATIONS = {
  short:   10 * 60,
  medium:  25 * 60,
  long:    45 * 60
};

const DEFAULT_SETTINGS = {
  priority: 'balanced',
  duration: TIMER_DURATIONS.medium
};
